export interface QueryParameters {
  measureValue: string
  cohortValue: string
  cohortTimeframeValue: string
  measureAggregationValue: string
}

export type TableStyle = "Absolute" | "Percentage"
export type QueryDisplayView = "TableView" | "LineChartView"
